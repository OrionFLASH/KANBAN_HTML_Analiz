"""
Пакет анализа Kanban-данных.

Структура:
- общие модули в корне `src/` (загрузка, фильтры, агрегация, …);
- `src.v1` — HTML + JSON + Excel (`run.py`, `config.json`);
- `src.v2` — Excel-only (`run_excel.py`, `config_excel_v2.json`);
- `src.Tests` — тесты.
"""
